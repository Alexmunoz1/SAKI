var images = document.getElementsByTagName('img');
for (var i = 0, l = images.length; i < l; i++) {
  images[i].src = '  images[i].src = '' + images[i].width + '/' + images[i].height;
' + images[i].width + '/' + images[i].height;
}
